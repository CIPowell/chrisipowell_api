language: elixir
elixir:
  - 1.3.4
otp:
  - 19.0.0

script:
  - mix local.rebar --force # for Elixir 1.3.0 and up
  - mix local.hex --force
  - mix deps.get
  - mix test
  - mix compile
  - mix release

notifications:
  email: false
